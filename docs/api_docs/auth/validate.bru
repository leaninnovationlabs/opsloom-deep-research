meta {
  name: validate
  type: http
  seq: 1
}

post {
  url: {{server}}/validate
  body: none
  auth: bearer
}

headers {
  access-token: {{token}}
}

auth:bearer {
  token: {{token}}
}

docs {
  # Overview
  User to login
}
